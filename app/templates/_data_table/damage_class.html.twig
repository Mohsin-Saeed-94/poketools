{#
/**
 * @var \App\Entity\MoveDamageClass value
 */
#}
{% if value %}
    <img class="pkt-icon" src="{{ asset('build/static/damage_class/'~value.slug~'.svg') }}"
         alt="{{ value.name }} class">
{% endif %}