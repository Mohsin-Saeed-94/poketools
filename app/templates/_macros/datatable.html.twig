{% macro datatable(data_table, table_id) %}
    <div id="{{ table_id }}"
         data-table-settings="{{ datatable_settings(data_table)|e('html_attr') }}">
        <i class="pkt-icon-loading"></i>
        Loading data...
    </div>
{% endmacro %}
